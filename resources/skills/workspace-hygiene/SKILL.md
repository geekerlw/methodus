---
name: workspace-hygiene
description: Keep task work inside the Methodus workspace; do not write Methodus home state or escape the workspace root.
---

# Workspace hygiene

Stay inside the current task workspace unless the user named an explicit project path.

- Do not modify `~/.methodus` except via Methodus itself.
- Prefer small, evidenced changes.
- Record commands and observations that support the result.
